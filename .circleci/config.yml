version: 2.1

jobs:
  build:
    docker:
      - image: openjdk:17
    steps:
      - checkout
      - run:
          name: Clean Build
          command: ./mvnw clean install
workflows:
  build-workflow:
    jobs:
      - build